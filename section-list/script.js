// const titles = document.querySelectorAll(".title");
// const getHandleScroll = elements => ev => {
//   const target = ev.target;
//   elements.forEach(element => {
//     if (target.scrollTop > element.offsetTop) {
//       element.querySelector("p").classList.add("sticky");
//     } else {
//       element.querySelector("p").classList.remove("sticky");
//     }
//   });
// };

// const list = document.querySelector("ul");

// list.addEventListener("scroll", getHandleScroll(titles));
